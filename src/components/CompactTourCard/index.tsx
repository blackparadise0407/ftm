import { memo } from 'react';
import './styles.scss';

interface CompactTourCardProps {}

export default memo(function CompactTourCard({}: CompactTourCardProps) {
  return (
    <div className="compact-tour-card">
      <img
        src="
      "
        alt=""
      />
    </div>
  );
});
